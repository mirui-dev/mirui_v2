<section class="flex max-width max-height">
    
    <!-- internals -->
    @livewire('internals.cart');

    <!-- contents-->
    @livewire('dashboard.nav')
    @livewire('dashboard.content')
    @livewire('dashboard.subcontentnav')
    @livewire('dashboard.subcontent')

</section>
